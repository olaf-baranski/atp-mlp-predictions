# ao2026-mlp-predictions
Machine learning project that predicts ATP men’s singles match winners (with a simple MLP neural network) using pre-match features. The model is trained on historical match data and used to generate predictions for the Australian Open 2026 draw.
